MatLabAdapter requires TickerServ to be run with Administrator privileges in order to connect to MatLab engine. In Windows 7 you have to explicitly select "Run as Administrator" no matter what type of account you are using.

For now, .m files should be put in the same directory as TickerServ.exe
